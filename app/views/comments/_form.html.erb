<%= form_for ([@product, @product.comments.build]) do |f| %>
<%= f.text_area :description, class:"single-input", placeholder:"enter comment"%>
<%= f.submit "product comment", class:"btn btn-pimary" %>
<%end%>